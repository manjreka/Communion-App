specVersion: "v1beta"
serviceId: "ui"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ui-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
